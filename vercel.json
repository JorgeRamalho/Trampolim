{
  "version": 2,
  "builds": [
    {
      "src": "backend/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/server.js" },
    { "src": "/app/(.*)", "dest": "/react/dist/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
